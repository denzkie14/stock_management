import 'package:flutter/material.dart';

class OrdrPage extends StatelessWidget {
  const OrdrPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
